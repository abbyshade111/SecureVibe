// Fixture: ordinary code with no secrets, used to check the scanner stays quiet here.
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export const MAX_RETRIES = 3;
export const homepage = 'https://example.com/about';
