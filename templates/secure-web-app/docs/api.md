# JSON API

The JSON endpoints this app exposes under `/api`. A full machine-readable description is in `docs/openapi.json`. Page routes (the parts of the app meant to be opened in a browser) are documented in docs/authorization.md.

| Route | Who may use it | Rate limit | CSRF token required | Idempotency-Key supported | What it does |
| --- | --- | --- | --- | --- | --- |
| GET `/healthz` | Anyone (no sign-in required) | general | No | No | Liveness check |
| GET `/readyz` | Anyone (no sign-in required) | general | No | No | Readiness check (loopback only) |
| GET `/account/export` | Any signed-in user | general | No | No | Download a copy of your data (JSON) |

---

*This file is generated by `npm run docs:build` from the app’s own code and configuration. Do not edit it by hand — change the code or configuration and run `npm run docs:build` again.*
